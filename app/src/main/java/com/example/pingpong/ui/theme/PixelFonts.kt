package com.example.pingpong.ui.theme

import androidx.compose.ui.text.font.FontFamily

// Safe fallback: use default system font to avoid any runtime issue if downloadable fonts
// provider is unavailable on the device/emulator. When a local TTF is bundled, switch to:
// import androidx.compose.ui.text.font.Font
// import com.example.pingpong.R
// val PixelFontFamily: FontFamily = FontFamily(Font(R.font.press_start_2p))
val PixelFontFamily: FontFamily = FontFamily.Default
